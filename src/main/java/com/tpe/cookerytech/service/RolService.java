package com.tpe.cookerytech.service;


import org.springframework.stereotype.Service;

@Service
public class RolService {

//    @Autowired
//    private LogRepository logRepository;
//
//    public Log findByType(LogType logType){

//        Log log = logRepository.findByType(logType).orElseThrow(()->
//                new ResourceNotFoundException(
//                        String.format(ErrorMessage.LOG_NOT_FOUND_EXCEPTION, logType.name())));
//        return log;
//    }
}
